# Chat UI Latency Tester

Simple HTTP latency tester for the `/configure_strategies` endpoint.

## Usage

```bash
poetry run python chatui/latency_tester.py
```

Edit `URL` in the script to point to your target endpoint.
